# Grade_System_python
A Python program that calculates student grades based on marks using conditional logic. The project also uses dictionaries to store grade ranges and demonstrates iteration over dictionary keys and values to determine the appropriate grade. Built as a beginner project to practice Python data structures and control flow.
